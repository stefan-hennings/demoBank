let startUrl = "http://localhost:8080";

let getAllAccounts = startUrl + "/account";
let addAccount = startUrl + "/account/add?holder=";
let login = startUrl + "/account/login?holderId=";
let deposit = startUrl + "/account/deposit?holderId="
let withdraw = startUrl + "/account/withdraw?holderId="